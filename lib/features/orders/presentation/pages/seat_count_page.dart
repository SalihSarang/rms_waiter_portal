import 'package:flutter/material.dart';

class SeatCountPage extends StatelessWidget {
  const SeatCountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
